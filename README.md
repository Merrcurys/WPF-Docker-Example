# Запуск WPF-приложения в Docker с помощью Wine

Этот проект демонстрирует, как запустить WPF-приложение, собранное под .NET Framework (в данном случае — .NET Framework 4.8), внутри Docker-контейнера с использованием Wine.

> ⚠️ **Важно**: WPF — это технология Windows Presentation Foundation, которая **не поддерживается на Linux**. Однако с помощью Wine (реализации Windows API для Unix-систем) можно попытаться запустить такие приложения в изолированной среде. Работоспособность не гарантируется и зависит от сложности UI и используемых компонентов.

---

## 🤨 Зачем?

Зачем вам это, когда уже есть Avalonia я не знаю, но лично мне задали это задание как дополнительное на смекалочку в моем техникуме по дисциплине Docker'а. Так как я изрядно помучался и поднял 1000 и 1 контейнер, прочитал тонну статей на StackOverfloy и другие подобных сайтах где явно писали "это не возможно". А также смотрел видео на YouTube, пытался гуглить на английском, на хинди и на китайском, где я не получил никаких результатов. Была еще идея использовать dockuur/windows, но мне нужна была KWM для этой реализации, да и образ был бы очень большим. Поэтому копал в сторону Wine. Сначала я пытался собрать все самому и под NET Core и под NET Framework. По итогу благодаря Scotty Hardy и его образу я наконец-то смог запустить WPF в Docker!

---

## 📦 Требования

- **Docker Desktop** - сам Docker демон, который должен быть запущен во время манипуляций.
- **Xming** - для отображения графического интерфейса, тоже должен быть запущен.
- **NET Framework** - я не знаю возможно ли запустить на NET Core, но лично я пробовал на NF 4.7.2 и NF 4.8.

---

## 🛠️ Подготовка

1. Убедитесь, что ваше WPF-приложение скомпилировано под **.NET Framework 4.8** и содержит исполняемый `.exe` файл (например, `WpfApp.exe`).
2. Удалите мое тестовое WPF приложение Biorhythms.
3. Добавьте в корень проекта ваше приложение, где также будет .exe и только он.
4. Запустите Docker Desktop и Xming если еще это не сделали.

---

## 🐳 Сборка образа

Выполните в корне проекта:

```bash
docker build -t wpf-wine-app .
```

---

## 🖥️ Запуск контейнера

```bash
docker run --rm wpf-wine-app
```

---

## ⚙️ Особенности работы

- **Графический интерфейс** может отображаться некорректно или медленно.
- **Мышь и клавиатура** могут работать нестабильно — используйте клавиши `Tab` и `Enter` для навигации по элементам. Или поищите зависимости, которые нужно установить, чтобы исправить это.
- Wine не поддерживает все функции .NET Framework. Приложения с тяжёлой графикой, анимациями или сторонними библиотеками могут не запуститься.

---

## 🔧 Советы по отладке

- Для более сложных сценариев может потребоваться установка дополнительных компонентов через `winetricks`.

---

> ✅ **Альтернатива**: Если вы разрабатываете новое приложение, рассмотрите переход на **.NET 6/8 с Avalonia UI** или **MAUI**, которые поддерживают кроссплатформенную разработку с нативным UI.